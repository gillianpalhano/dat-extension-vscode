# Install
``` bash
# Instale o Yeoman e o Gerador de Extensões do VSCode:
npm install -g yo generator-code

# Execute o Yeoman para criar a estrutura da extensão (primeira vez):
yo code

# Instale dependencias
npm i


# Instale a ferramenta vsce para empacotar sua extensão:
npm install -g vsce
# Gere o pacote da extensão:
vsce package
```